var express = require("express");
var bodyParser = require("body-parser");
var markdown = express.Router();

markdown.post("/", function(req, res){

});

markdown.get("/", function(req, res){

})

module.exports = markdown;
